---
- name: Install basic utilities
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - vim
    - tmux
    - curl
    - wget
    - htop
    - zip
    - unzip
    - pigz
    - pip # !Improtant, for pymysql